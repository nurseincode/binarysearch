# Implementation of binary seary algorithm
# Prove that binary search is faster than naive search
# Naive search: scan entire list and ask if its equal to the target
 - If yes, return the index
 - If no, the return -1
 